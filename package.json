{
  "dependencies": {
    "@angular/cli": "^20.3.3"
  },
  "name": "site",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run ng serve",
    "build": "bun run ng build",
    "test": "bun run ng test",
    "lint": "bun run ng lint",
    "e2e": "bun run ng e2e"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
